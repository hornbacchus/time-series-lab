# Phase 5 Close redirected (mit-iii) construction [PRE-FLIGHT] — successor-utility structure audit per Q-mit-iii-2 reversal

**Date:** 2026-05-08
**Scope:** Redirected pre-flight per chat-instance-seam
recalibration (`55c00d9` + `398d1ff`); Q-mit-iii-2 reversal
codified successor-utility ordering replacing 5-section
cataloging structure. Audits each section against Phase 6+
inheritance test ("would a Phase 6+ Code instance with no
Phase 5 context use this section to ship their first sub-
session?"). Mitigation activation (mit-ii) + (mit-iv) applied.
**Status:** COMPLETE.

## §1 Quick-start section audit (Category 1)

**Content scope:** repo clone + working directory + pre-commit
gates + sub-session opening (read trigger + read prior
closeout + verify working tree clean + verify master HEAD) +
first commit pattern (HEREDOC + co-author trailer + push) +
closeout protocol (commit SHA + workflow run ID + CI green
confirmation).

**Phase 6+ inheritance test:** PASS-deterministic. Concrete
action sequence with exact commands enables first-sub-session
shipping. No Phase 5 context required to execute.

**LOC projection:** ~50-80 LOC.

## §2 Operational discipline section audit (Category 2)

**Content scope:** chunking thresholds (§13.1 default 200 +
§13.4 hard 220 + 200-220 marginal-tolerance band + (mit-i)
cascading split pre-authorization) + smoke test semantic
decision tree by invariant class (closed-form deterministic
PASS / MCMC stochastic loose / INVERTED orthogonality
PASS-with-checker-handles) + CI verification protocol
(commit-attributable vs environmental per Q-S5-CI-environmental-1=
(β) caveat) + pre-commit gates (parity-fast --check-environment
+ pytest + structural invariants + install-matrix).

**Phase 6+ inheritance test:** PASS-deterministic. Phase 6+
Code instance can ship a first commit knowing chunking + smoke
test + CI verification + pre-commit gates from §2 alone.

**LOC projection:** ~80-120 LOC.

## §3 Pattern recipes section audit (Category 3)

**Content scope:** per-wrapper integration recipe (Cases
0/(i)/(iii) empirically observed; Cases (ii)+(iv) reserved) +
cross-wrapper acceptance recipe (per-class aggregation per S5
3-class structure) + banking-when-warranted recipe per
Q-banking-categorical strict.

**Worked examples** drawn from Phase 5 sub-sessions:
- S3 MCMC SV ESS empirical finding (Case 0; loose-assertion
  smoke test bifurcation banking) — exemplifies Case 0 +
  smoke test class divergence
- S4-α `3e_mint_family` Case (i) FIRST observation
  (representative-choice = mint_shrinkage) — exemplifies
  Case (i) representative-choice variant
- S4-β `3f_transformer_attention` Case (i) CONSECUTIVE
  observation (representative-choice = Layer 0) — exemplifies
  consecutive Case (i) + verdict_class orthogonality
- S4-γ `3a_caviar_sav` INVERTED Case (i) variant (rename
  mapping `christoffersen_pval` → `chris_pvalue`) —
  exemplifies INVERTED + Case (i) rename mapping variant
- S5 cross-wrapper acceptance dispatch test framework —
  exemplifies per-class aggregation across 3 invariant
  classes

**Phase 6+ inheritance test:** PASS-deterministic. Phase 6+
Code instance can integrate a new wrapper using §3 recipes +
worked examples without enumerating prior cycle history.
Recipe-with-example pairing is the most actionable section
for first-time sub-session execution.

**LOC projection:** ~120-180 LOC (content-heavy; 5+ worked
examples).

## §4 Banking pointer index section audit (Category 4)

**Content scope:** searchable "for X situation, see banking
Y" structure rather than enumerated entry table.

**Index structure (situation-keyed):**
- "For chunking threshold engagement" → Q-OVERSHOOT-PREFLIGHT-
  RECALIBRATION-V1+V2 + Q-OVERSHOOT-EXECUTION-RECALIBRATION
- "For Case (i) representative-choice variant" → S4-α +
  S4-β findings docs banking inline
- "For Case (i) rename mapping variant" → S4-γ findings
- "For CI environmental failure handling" → S5 findings +
  Q-S5-CI-environmental-1=(β) caveat
- "For drift-evidence mitigation activation" →
  PRE-FLIGHT-MITIGATION-PATH-ACTIVATION
- "For lifecycle method signature multi-side extension" →
  S2-α-2-redux LIFECYCLE-METHOD-SIGNATURE-EXTENSION
- "For master plan amendment per pre-flight finding" →
  MASTER-PLAN-V1.1-§15-S3-FRAMING-RECONCILIATION
- (additional pointers covering ~30+ banking surface area)

**Phase 6+ inheritance test:** PASS-deterministic if pointer
coverage spans cycle's banking surface. Phase 6+ Code instance
encountering a novel situation finds relevant prior context
via situation-keyed access pattern in <2 minutes (vs
enumerating ~30+ banking docs).

**Architectural decision pending construction:** pointer
coverage scope. Code's recommendation: cover all situations
where Phase 6+ Code might surface ambiguity (chunking,
overshoot, environmental failure, Case determination,
mitigation activation, master plan amendments, recalibrations).
Comprehensive coverage scope determined at construction with
~15-25 pointer entries; not exhaustive enumeration.

**LOC projection:** ~60-100 LOC.

## §5 Reference section audit (Category 5)

**Content scope:** master plan v1.2 link (authoritative) + §15
sub-domain framing summary + §17 + §18 closure note context +
appendix:
- 8-wrapper allowlist final state with Cases per wrapper
- Reserve sub-domains (ii)+(iii)+(iv) framing
- Q-mit-iii-1=(α) comprehensive scope preserved AS REFERENCE
  CONTENT (cycle-architecture refinements; dispositional
  framework patterns; cycle-establishment context)

**Phase 6+ inheritance test:** PASS-deterministic without
duplicating §1-§4 operational content. Reference depth without
operational redundancy. Q-mit-iii-1=(α) comprehensive scope
preservation honored without compromising successor-utility
ordering.

**LOC projection:** ~40-60 LOC.

## §6 Cross-section navigation + LOC projection + chunking disposition (Category 6)

**TOC structure with section anchors.** Cross-references:
- §3 pattern recipes → §4 banking pointer index ("see banking
  Y" navigation)
- §1 quick-start → §2 operational discipline (forward
  reference for chunking + smoke test depth)
- §2 → §3 (operational discipline applied via pattern
  recipes)
- §4 → §5 reference for master plan link

**Combined construction projection:** **~350-540 LOC**
(§1 50-80 + §2 80-120 + §3 120-180 + §4 60-100 + §5 40-60).

**Chunking disposition:**
- Lower bound ~350 LOC: ~59% over §13.4 hard threshold
- Upper bound ~540 LOC: ~145% over hard threshold
- Cascading split mandatory per (mit-i) standing

**Three structural options:**
- (split-A) 5-commit (1 per section) — finest granularity;
  each section commit clean per §13.1 default
- (split-B) 2-commit (§1+§2 / §3+§4+§5) — operational vs
  reference seam
- (split-C) 3-commit (§1+§2 / §3+§4 / §5) — middle granularity

**Code's recommendation: (split-A) 5-commit split** for
finest granularity given each section structurally distinct.
Per-section LOC: §1 ~50-80 + §2 ~80-120 + §3 ~120-180 + §4
~60-100 + §5 ~40-60 — each clean per §13.1 default, with §3
potentially engaging marginal-tolerance band at upper bound.

Construction sub-session chunking determined at Chat
disposition post-pre-flight closure.

## Disposition

Redirected (mit-iii) construction [PRE-FLIGHT] audit COMPLETE.
5 successor-utility sections audited against Phase 6+
inheritance test (all PASS-deterministic); cross-section
navigation specified; combined construction ~350-540 LOC;
cascading split mandatory; Code recommends (split-A) 5-commit
split for finest granularity.

Architectural decision pending Chat disposition: chunking
choice (split-A vs split-B vs split-C) + §4 banking pointer
index coverage scope. (mit-iii) construction execution sub-
session ahead per Q-S4-2=(α) sequential standing.

**§13.4 compliance:** Pre-flight commit delta verified at
staging time per Code's chunking judgment.
